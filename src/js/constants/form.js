import { findElement } from '../utils/helpers';

const FORM_SIGN_IN = {
  form: '#formSignIn'
}

const FORM_SIGN_UP = {
  form: '#formSignUp'
}

const FORM_SEARCH = {
  form: '#search'
}

export {
  FORM_SIGN_IN,
  FORM_SIGN_UP,
  FORM_SEARCH
}
